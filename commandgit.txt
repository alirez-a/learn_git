#start git in the folder
git init

#show log
// status

#edit file or folder
// add (name,-A) 
// commit -m "comment"

#show changes in edits
// diff head #last committed
// diff --staged

#go back in log
// reset namefile
// checkout -- namefile

#branch
// branch
// branch name
// branch checkout name
// branch -d name
// merge name
// branch -M new_name 
// branch -m new_name 

# edit global
// clone link
// remote add origin link 
// push origin master (send to origin)
// pull origin master (get from origin)

# show file in the branch
cat (name) 